# Copy to .env and fill in each field. Never commit your real secrets.

# Base URL for your ntfy server. Leave as https://ntfy.sh for the hosted service.
NTFY_BASE_URL=https://ntfy.sh

# REQUIRED: high-entropy topic string that both Codex and the ntfy app use.
NTFY_TOPIC=codex-notify-CHANGE-ME

# Optional bearer token if your ntfy topic is locked down.
NTFY_TOKEN=

# Default priority applied when Codex omits one. Allowed values: min, low, default, high, urgent.
NOTIFY_DEFAULT_PRIORITY=default

# Optional tag/category to attach to every notification.
NOTIFY_DEFAULT_CATEGORY=
